# Mechanical assembly

Root link(s): `base`

1. Attach **FL_wheg** to **base** via joint `FL_WHEG_drive` (continuous).
2. Attach **FR_wheg** to **base** via joint `FR_WHEG_drive` (continuous).
3. Attach **RL_wheg** to **base** via joint `RL_WHEG_drive` (continuous).
4. Attach **RR_wheg** to **base** via joint `RR_WHEG_drive` (continuous).
